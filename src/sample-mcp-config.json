{
  "mcpServers": {
    //#if (TransportMethod == "streamable-http")
    "McpServerTemplate-http": {
      "url": "http://localhost:5000/mcp"
    }
    //#else
    "McpServerTemplate-stdio": {
      "command": "dotnet",
      "args": [
        "run", 
        "--project", 
        "McpServerTemplate.csproj"
      ],
      "env": {},
      "// Note": "Update the project path above to match your actual project location if needed"
    }
    //#endif
  }
}
